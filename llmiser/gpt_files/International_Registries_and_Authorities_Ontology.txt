
Business identifier code data record is defined as entry in a registry that conforms to ISO 9362 for the management of BIC codes and related registration information. Business identifier code data record is a kind of business registry entry. It comprises some business identifier code. It is included in value business identifier code registry. It is self-maintained some string.
Market identifier code registry entry is defined as entry in a market identifier code registry that conforms to ISO 10383. Market identifier code registry entry is a kind of registry entry. It comprises some market identifier. It has initial registration date exactly one string. It has market identifier code status exactly one market indicator code status. It has registration status revision date at most one string. It is included in value market identifier code registry.